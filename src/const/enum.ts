export enum Position {
    FIXED = 'fixed',
    ABSOLUTE = 'absolute',
    RELATIVE = 'relative',
    STICKY = 'sticky',
    STATIC = 'static'
}

export enum ewMessageEnumType {
    success = "success",
    info = "info",
    warning = "warning",
    error = "error",
}